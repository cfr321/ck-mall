server:
  port: 10086
spring:
  application:
    name: ck-registry
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false   #不注册自己
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka